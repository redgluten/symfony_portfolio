{% extends "IPUserBundle::layout.html.twig" %}


{% block content %}

    <div class="jumbotron">
        <h1>Hello {{ name }}!</h1>

        <p>Lorem ipsum Aute id labore proident non ut consectetur ex do non velit sunt nostrud in laboris pariatur irure enim sint Duis in adipisicing consequat dolor laboris est elit.</p>

        <p><a href="#" class="btn btn-lg btn-primary">Boutonne moi !</a></p>
    </div>

    {% if users|length > 0 %}
        <section>
            <h1>Utilisateurs</h1>

            <ul>
                {% for user in users %}
                    <li>{{ user.name|e }}</li>
                {% endfor %}
            </ul>
        </section>
    {% endif %}

{% endblock %}
